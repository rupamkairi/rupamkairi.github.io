{
  "name": "rupamkairi.github.io",
  "description": "",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev-web": "lerna bootstrap && (cd web && npm run dev)",
    "dev-cm": "lerna bootstrap && (cd cm && npm run start)",
    "dev": "lerna run dev --parallel",
    "build-web": "lerna bootstrap && (cd web && npm run build)",
    "build-cm": "lerna bootstrap && (cd cm && npm run build)",
    "build": "lerna run build --parallel",
    "gen-web": "lerna bootstrap && (cd web && npm run generate)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rupamkairi/rupamkairi.github.io.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rupamkairi/rupamkairi.github.io/issues"
  },
  "homepage": "https://github.com/rupamkairi/rupamkairi.github.io#readme",
  "devDependencies": {
    "@sanity/cli": "^2.1.0",
    "lerna": "^3.22.1"
  }
}
